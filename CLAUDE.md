# CLAUDE.md

> Claude Code はセッション開始時にこのファイルを自動で読む。

## この案件について

- 案件名: メモおこし（memo-okoshi）— 手書き面談メモの写真を構造化記録へ変換する「変換器」
- 種別: 個人開発（試験開発。形になったら法人PT・県協会提示物へ昇格予定）
- 台帳: memo-okoshi-handoff.md

## セッション開始時に必ずやること

1. 上記の台帳（handoff）を読み、現在地・決定事項・教訓を把握する
2. 作業規範は dev-workflow スキル（~/.claude/skills/dev-workflow/）に従う
3. 把握が終わるまで、実装・編集・デプロイをしない（鉄則1）

## 台帳の維持（肥大させない）

台帳は**毎セッションが起動時に読む**。肥大するとコンテキストを毎回浪費する。
台帳には**「いま効いていること」だけ**を置き、**「なぜそうしたか」はアーカイブ**に置く。
⚠️ **要約・圧縮による削減は禁止**（アーカイブは原文保存が目的）。**移すのであって、削るのではない。**

**台帳全体が 4.0万文字を超えたら点検する**（下の A・B・D の分量を数える）:
- **記録 0.9万字以内（かつ10件以内）** **かつ** 「いま守る制約」1.2万字以内 → 規則は守られている
- どちらかが超過 → 超過したほうを、その節の手順でアーカイブへ移す
- どちらも規定内なのに超えているなら、増えているのは**生きている注意事項**（D を読む）
- ⚠️ **件数だけを見ない。** 2026-09-11、**記録10件を守ったまま1件が約2千字に育ち**、
  台帳が5.08万字まで膨らんだ。分量は件数と別に数える。
- ⚠️ 文字数は **`LC_ALL=ja_JP.UTF-8 wc -m`** で数える。ロケール指定なしの `wc -m` は
  **バイト数**を返し、日本語では約3倍の値に見える（2026-09-11に一度これで誤読した）。

※ 目安の内訳（2026-09-11の整理後）: 制約 1.20万＋記録 0.86万＋注意事項 0.88万＋
  地図・ロードマップ・現在地・索引 0.97万 ≒ **3.9万文字**。4.0万までの余りが少ないので、
  **次に超えたら D（生きている注意事項。終わったものを消す）から当たる**こと。

### A. 記録（フェーズ/セッションの作業ログ）

- **直近10件まで。**
- ⚠️ **記録の本文には「何をなぜ決めたか」だけを書く。表・実測値・検証結果は書かない。**
  次のセッションが読むときに要るのは**判断の中身**で、表や実測は判断の**根拠**であって結論ではない。
  - **書く**: 判断／その理由／踏んだ落とし穴（⚠️）／`[DECISION]`／
    末尾に**テスト件数と commit の1行**（追跡のため）。
  - **書かない**: 比較表・寸法や秒数の実測・画面幅ごとの確認結果・実機確認の手順と結果。
  - ⚠️ **制約そのものを書き写さない。** 守るべきことの正本は「いま守る制約」なので、
    記録には**なぜそう決めたか**だけを書き、必要なら主題名で参照する（**同じ文を2か所に置かない**）。
- **表・実測・検証は `memo-okoshi-archive-YYYY-MM.md` の `# 記録の全文` へ。**
  ⚠️ 表だけを切り出さず、**その回の記録の全文を原文のまま**置く（表だけでは何のための測定か分からない）。
  台帳の記録には **`- 検証: → アーカイブ「記録の全文 → 日付 フェーズ名」（何が載っているか）`** の1行を置く。
  測ったものが無い回（規則の見直しなど）は、この行もアーカイブへの写しも要らない。
- **分量の目安: 1件 900文字・記録全体で 0.9万文字。** 超えたら長い記録から判断以外を落とす。
- **11件目を足すときは、最も古い1件を台帳から消すだけでよい**（全文は書いた時点でアーカイブにある）。
  消す前に、その中の**いまも効いている制約**（確定値・禁則・落とし穴）が「いま守る制約」または
  「生きている注意事項」にあることを確かめる（無ければ昇格させてから消す。迷ったら残す側に倒す）。

### B. 決定（[DECISION]）

⚠️ **決定はフェーズ別に積まない。** 「P6-b の決定」「P7 の決定」のように回ごとの節を作ると、
件数制限が効かないまま無限に増える（2026-09-09にこれで台帳が4.6万文字まで膨らんだ）。
台帳に置くのは **`## いま守る制約（主題別）`** ただ一箇所で、**主題別**（全体の作法／工程1〜5／
辞書／使い方ページ 等）に畳む。新しい決定は**該当する主題の節へ追記**する。

- **分量の目安: 「いま守る制約」の合計が 1.2万文字。** 超えたら整理する。
- **超えたときの扱い**（上から順に試す）:
  1. **同じ主題の決定をまとめる。** 後の決定が前の決定を狭めた／上書きしたなら、
     **結論の1行に統合**し、経過はアーカイブへ。**条件変更は上書きせず、日付つきで追記**する
     （例: 「モデル名を固定しない（2026-08-17 →条件変更 2026-09-09）」）。
  2. **経緯（下記の見分け方で「経緯」に落ちたもの）をアーカイブへ原文のまま移す。**
     移す先は `memo-okoshi-archive-YYYY-MM.md` の `# 決定の経緯（台帳から移動）`。
  3. それでも収まらなければ、**主題ごと**アーカイブへ移し、台帳には見出し1行＋参照だけ残す。

### C. ⚠️ 最優先: いま効いている決定を見えなくしない

分量を減らすことより、**守るべきことが読み落とされないこと**が上。
1行ずつ、次の3つで「制約」と「経緯」に振り分ける。

| | **制約 →台帳に残す** | **経緯 →アーカイブへ** |
|---|---|---|
| ① これから書くコード・画面を**縛るか** | 縛る（〜する／〜しない／値が決まっている） | 縛らない（当時こうだった、という報告） |
| ② 破ったら**何が起きるか** | 設計が壊れる・個人情報が出る・テストが落ちる | 見た目や手順が変わるだけ |
| ③ **いつの話か** | いまのコードに対応物がある | すでに作り直された画面・却下した案・当時の実測値 |

- **①が「縛る」なら、②③に関わらず台帳に残す。**迷ったら残す側に倒す。
- 台帳に残す形は**命令形の1行**（理由は書かない。理由を知りたい人はアーカイブを読む）。
  経緯を切り離せない制約は、**制約を先に1行で書き、そのあとに理由を1文だけ**添える。
- **テストが見張っている制約は、台帳は1行＋テストファイル名でよい**（正本はテスト）。
  例: 「赤が残る間は完成不可（`tests/invariants.test.mts`）」。
  逆に、**テストが無い制約は本文のまま台帳に残す**（機械が守ってくれないから）。
- **不可侵の設計原則5点**（台帳の冒頭）は、この整理の対象外。**何があっても台帳の先頭に置く。**
- 移したあとは必ず**自己点検**する: 台帳だけを読んで
  ①現在地 ②不可侵の原則 ③いま守る制約 ④生きている注意事項 ⑤次の一手 が分かるか。
  分からなければ移しすぎ。**アーカイブから戻す。**

### D. 生きている注意事項

- ここは**まだ終わっていないこと**を置く場所（未解決の課題・未実装・積み残し・運用手順）。
- **終わったら消す。** 解決した課題・実装した機能・確認の済んだ積み残しは、
  **その回の記録に書いてから注意事項から外す**（記録は残るので失われない）。
  「念のため残す」を続けると、次のセッションが**まだ効いていない警告を守ろうとする**。
- 「終わったら消す」が効くのはここだけ。**Bの制約とCの原則5点は、終わらないから消さない。**

## このリポジトリ固有の注意

- デプロイ方法: **Vercel CLI から**（GitHub連携なし。push だけでは本番に反映されない）。
  `npx -y vercel@latest deploy --prod --yes`。手順と注意は台帳の「本番（Vercel）の運用」を読む。
- **UI仕様の正本は実装そのもの**（`app/components/` ＋ `app/globals.css`）。2026-09-07に移管。
  画面を変えるときは実装を直す。モックへ反映し直す作業は不要。
- **工程名の表示は「伏せる」**（2026-09-09に「黒塗り」から改称）。画面に出る文言だけを変えてあり、
  **コード内の識別子は `mask` / `Redact` / `kuronuri-mock` のまま**（無用な差分を増やさないため）。
  UI文言に「黒塗り」が残らないことは `npm test`（tests/ui-text.test.mts）が検査する。
  ※台帳・過去の記録に残る「黒塗り」は当時の記述。**書き換えない**。
- 触ってはいけない場所:
  - **docs/mock/*.html は編集禁止**（memo-okoshi-mock-v6／kuronuri-mock-v1／gate-mock-v1／
    torikomi-mock-v1）。設計意図の記録として凍結保存する（実装へ移植済み。今後更新しない。
    実装と食い違ったら実装が正しい）。
    ※ torikomi-mock-v1 の**並べ替えはモックが誤り**（↑↓）。正しいのは実装の ←→ ＋ ドラッグ。
- その他:
  - **開発検証はダミーメモのみ。実際の利用者情報を含む画像・テキストをリポジトリにもテストにも絶対に入れない**。
  - APIキー・シークレットをコミットしない（P1以降。環境変数で扱う）。
