# メモおこし（memo-okoshi）台帳 — handoff

> **読む順序: ① 不可侵の設計原則5点 → ② 現在地・次の一手 → ③ 生きている注意事項 →
> ④ いま守る制約（触る主題だけでよい）→ ⑤ CLAUDE.md → ⑥ 必要なら「ファイルの地図」の該当ファイル。**
>
> **これが本案件の正本台帳**（初版 2026-08-17）。新チャットは必ずここから読み始める。
> 台帳には**「いま効いていること」だけ**を置き、**「なぜそうしたか」は `memo-okoshi-archive-YYYY-MM.md`**
> に原文で置く（2026-09-09に整理）。守るべきことの正本は **「不可侵の設計原則5点」＋「いま守る制約（主題別）」**。
> 維持規則は CLAUDE.md の「台帳の維持」に従う
> （記録は直近10件／いま守る制約は1.2万字／台帳全体4.0万字で点検／**要約でなく原文アーカイブ**）。

---

# 【現在地サマリ】

## ⚠️ 不可侵の設計原則5点（触れる変更は実装せず、報告して停止する）

> 打ち合わせで積み上げた [DECISION] であり、**「良かれと思って」緩めることを禁じる**。
> 関わるアイデアが出たら**実装前に報告して停止**すること。既存の不変条件テストは常に全通過を維持する
> （壊れたら止まって報告）。台帳の他の記述がこの5点と食い違って読めるときは、**この5点が優先**。

1. **記録と助言を混ぜない** — 気づき（insights）／こぼれ（spill）が転記テキスト・項目コピーに混入しない
2. **黙って捨てない** — メモの内容は必ずどこかに残る（項目・こぼれ枠のいずれか）
3. **赤マーカーは強制** — 未解決なら完成も項目コピーも不可。緩めない
4. **説明文を足さない** — 常時表示の注記・凡例・ラベルを増やさない。
   説明はホバー/フォーカス・初回のみ・要求時のみ（引き算原則）
5. **サーバーに保存しない** — DBを持たない変換器。マスク前の画像を端末外に出さない

（この5点は2026-09-09まで指示書の中にしかなく、台帳には番号での参照だけがあった。
　いま効いている決定が台帳から見えない状態だったため、原文のまま台帳の先頭へ移した。）

## 位置づけ

- 個人の試験開発。形になったら法人PT・県協会提示物へ昇格。学習目的＝画像→構造化のマルチモーダル実践。
- 名称「メモおこし」。**主戦場は帰所後のPC編集**（スマホは取り込み側が中心）。

## 現在地

- **P0〜P7-d 完了**（2026-09-10）。**テスト90件通過。**
  各回に何をしたかは下の**ロードマップ**が1行ずつ持っている。詳細は「記録」とアーカイブ。
- **本番デプロイ済み**: 最新は **P7-d**（2026-09-10・実装の commit `%C%`）。
  **https://memookoshi.fknd.jp**（配布用。`memo-okoshi.vercel.app` も有効）。
  デプロイ後の確認（毎回この5点）: `/` 307→/gate・`/gate` 200・`/about` 200・
  未認証 `POST /api/convert` 401・robots は Disallow。
  中身は **`_next/` がゲート対象外**であることを使って確かめる（手順は「本番（Vercel）の運用」）。
  P7時点の手元ビルド＝本番ビルドの一致を `f8c9310fd8def414.css` で確認済み。
- **台帳の構造を2026-09-09に整理した**（決定をフェーズ別から主題別へ／原則5点を先頭へ／原文はアーカイブへ）。
  書き足すときは CLAUDE.md「台帳の維持」を先に読むこと。

## 直近の決定

- 決定の正本は下の **「いま守る制約（主題別）」**（2026-09-09にフェーズ別の節をここへ畳んだ）。
  2026-08-17 に確定した「決定事項リスト」は**当時の合意の記録**として
  `memo-okoshi-archive-2026-09.md`（決定の経緯）へ移してある。
- 決定の書き足し方・整理の仕方は **CLAUDE.md「台帳の維持 → B. 決定／C.」** を読むこと。

## 次の一手

1. **【最優先】P1-b: 実物の走り書きでの読み取り検証**。本番URL（https://memookoshi.fknd.jp）に iPhone から
   アクセスし、カメラ経路で撮影→黒塗り→変換で実施する（ダミー・伏せ字のメモで。実データは使わない）。
   見るべき点: **黄マーカーが何件出るか**（下記「未解決の課題」の1つ目に直結）、赤の検知、発言者の取り違え、
   崩し字の読み取り精度。辞書に事業所の略語を数語入れてから試すと効果が見える。
   → **この結果を見てから、黄マーカーの揺れへの対応を設計側が判断する。**
2. **Windows PC での実機確認**（下記「実機確認の積み残し（Windows）」）。開発機が Mac のため未確認。
3. **実機調整**: 「実機で調整が必要な値（P2）」の表を iPhone/iPad で当たる。
4. **AI委員会での試用開始**（合言葉は別経路で配布）。規程整理と並行。
5. P8: 現場試用の判断・規程整理（Gemini有料設定の切り替えはここで）。※旧「P7」。2026-09-09に改番。

## 生きている注意事項

### ⚠️ 未解決の課題（2026-09-08時点・着手前に設計側の判断が要る）

- **黄マーカーの件数が呼ぶたびに揺れる（0〜2件・2026-09-09 実測）**。**未解決のまま**。
  当初の見立てだった「モデルが毎回変わるから」は **P7 で潰した**（優先モデル＋temperature 0 で、
  同じ画像5回すべて `gemini-3.6-flash`）。それでも黄は **0 / 2 / 1 / 2 / 0** と揺れる。
  つまり**残っているのは生成そのもののばらつき**で、モデル差ではない。
  **黄が0件になった回は「自信ありげに誤読を通す」状態**であり、設計の核（読めない箇所を申告させる）に
  関わる懸念として残る。
  → **P1-b（実物の走り書きでの検証）の結果を見て、設計側が対応を判断する。勝手に着手しないこと。**
  → 判断の材料になる選択肢（いずれも未決定）: プロンプトで黄の出し方を明示的に指示する、
    複数回呼んで比較する、黄0件のときに画面で注意を促す、など。
  ※ **項目の振り分け**（どの内容がどの項目に入るか）の揺れは P7 で収まった
    （下の「いま守る制約 → 工程3 変換」。実測値はアーカイブの「決定の経緯 → P7 の決定」）。
    揺れているのは黄の件数だけになった。

- **Windows 実機での未確認事項**が残っている（→ 下記「実機確認の積み残し（Windows）」）。会社PCはWindowsのため、
  配布前に一度は当てる必要がある。

### ⚠️ 無料枠の上限（2026-09-10 実測・配布前に効いてくる）

- ⚠️ **1日20リクエスト／モデル／プロジェクト**（`GenerateRequestsPerDayPerProjectPerModel-FreeTier`・
  `quotaValue: 20`）。優先リストは2つなので、**1日で合わせて40変換が上限**の目安。
  → AI委員会の試用で複数人が同じ日に使うと、この上限に当たる。**有料切り替え（P8）の判断材料。**
  - **対処済み（2026-09-10・P7-d）**: 枠切れのときに**一覧の他のモデルへ流さない**ようにした。
    以前は21回目から 3.6-flash に流れ、**気づかないまま「読めない箇所を申告しない」状態**に落ちていた。
    いまは画面に「本日の利用上限に達しました。明日また試してください。」と出して止まる。
    **入力（画像・伏せた箇所・辞書）は残り、その場で再試行できる。**
- ⚠️ **HTTP 503（混雑）で失敗した回も1日20回に数えられている**（再試行するほど枠が減る）。
  計測で再試行を重ねたところ、成功0回のまま 3.8-flash の枠を使い切った。
- ⚠️ **Pro系（`gemini-3.1-pro-preview` / `gemini-pro-latest`）は無料枠では呼べない。**
  2トークンの ping でも 429 で、`GenerateContentInputTokensPerModelPerDay-FreeTier` に当たる
  （`gemini-pro-latest` は `gemini-3.1-pro` と同じ枠）。**上位モデルの検証には有料切り替えが要る。**
  `gemini-2.5-pro` は 404（新規ユーザーには提供終了）。`gemini-3.5-transcribe` は画像入力に非対応。
- `gemini-flash-latest` は最新の flash と**同じ枠**を使う（別名なので枠は増えない）。

### 未実装・保留（触るときに読む）

- **マーカー↔元メモの位置連動は未実装**（APIが `ref` を返さないため。2026-09-07のP3でスコープ外とした）。
  つなぐためのフックだけ残してある（`Token.ref` ／ `Review.flashMemo` ／ `public/dev-fixture.json` の ref 値）。
  → 実装したら、元メモペインの ? を「伏せたあとの画像を表示。マーカーを押すと該当行が光ります」へ戻すこと
  （いまは「伏せたあとの画像を表示」まで**に短縮してある**）。
- ⚠️ **iOS のホーム画面アプリ（standalone）は Safari と保存領域が分かれる。**
  Safari で登録した**辞書と項目設定はホーム画面アプリ側に引き継がれない**。移すときは辞書の書き出し／読み込みを使う。
  **配布時に一言添えること。**
- **HEIC（iPhone写真）**: 変換ライブラリは入れていない。iOS Safari は input 経由で JPEG に変換して渡してくる
  （要実機確認）。PC の Chrome に HEIC を直接入れると復号できず「読み込めませんでした: ファイル名」のトーストになる。
- [DECISION保留] **Gemini API の無料枠は入力データが Google のサービス改善に利用される規約。**
  ダミー検証（P1〜P4）は問題なし。実データ利用を検討する **P8（旧P7）では有料設定への切り替えが前提条件**。
  規程整理時に必ず確認すること。
- **ダミー素材の限界**: 「複数画像＝1件の記録」を正しく検証するには**同一面談の複数ページ素材**が別途必要。
  01〜03 は別々の児童・別日の面談で、この検証には使えない。
  （成人向け項目の噛み合わせは 2026-09-10 に `dummy-memo-04-hard`（成人・就労/金銭管理）を作って解消した。）
- **予備の変換先として Claude API を想定してある**（2026-08-17の決定。未実装）。Gemini が使えなくなった場合の逃げ道。
- **ダミーは字が整いすぎている。本物の走り書きでの検証は未実施**（P1-bの宿題。上の「未解決の課題」に直結）。

### 実機確認の積み残し（Windows・開発機がMacのため未確認）

- 黒塗り: **マウスホイール=拡大／トラックパッド2本指=移動** の切り分け（deltaX の有無で判定）が Windows の
  精密タッチパッド・Edge/Chrome で意図どおりか。Space+ドラッグ・中ボタンドラッグ・移動ボタンの手応え
- Word: 游ゴシックでの見え方、表の罫線、余白1.5cm、本文コピペ時に余計な改行が入らないか（Windows版Word）
- PDF: Edge/Chrome の「PDFとして保存」でファイル名が `メモおこし_日付` になるか、A4・余白15mm・改ページ
- 取り込み: PC判定でカメラ経路が出ないこと、エクスプローラーからのドラッグ＆ドロップ、HEIC を直接入れた時の
  「読み込めませんでした」表示
- beforeunload の文言はブラウザ既定（カスタム文言は出せない仕様）
- 合言葉画面: Edge でのオートフィル（autocomplete=current-password）の挙動
- （P6-b追加）ツールチップの向き: ヘッダー＝下向き・右端の道具列＝右合わせ が Edge/Chrome で切れずに出るか
- （P6-b追加）アイコンだけのボタン（⧉/✎/←→/×）がタッチ端末で職員に伝わるか（伝わらなければ初回のみラベル表示を検討）
- （P6-d追加）**iOSのホーム画面に追加したときの実機確認**（この Mac には Xcode もシミュレータも無く、
  180pxの原寸＋iOSと同じ角丸をかけた再現までしか確認できていない）。あわせて standalone 起動時に
  戻り導線だけで詰まらないか、iOSで辞書が引き継がれない件が実際どう見えるかも見る
- （P6-c追加）取り込み画面のドラッグ並べ替え: Windows のマウス／精密タッチパッドでの掴み心地、
  つまみ（⠿）が「掴める」と伝わるか。実機タッチ（iPhone/Android）でつまみからのドラッグが
  机のスクロールと競合しないか。紙が多いとき（端での自動スクロール）の使い勝手

### 実機で調整が必要な値（P2）

| 値 | 場所 | 初期値 | 何に効くか |
|---|---|---|---|
| ペン太さ s/m/l | app/components/Redact.tsx `PEN` | 画像幅の 1.2% / 2.5% / 4.5% | 指で名前1語を一発で消せる太さか |
| 消しゴム倍率 | 同 `ERASER_RATIO` | ペンの1.6倍 | 塗りすぎを戻しやすいか |
| 全体表示の余白 | lib/stage.ts `FIT_PAD` | 14px | 初期表示の大きさ |
| 拡大の刻み | useZoomPan（ホイール1.12倍・ボタン1.5倍・上限8倍） | — | 指/ホイールの手応え |
| 初回案内の表示秒数 | Redact `5200ms` | 5.2秒 | 読めるか・邪魔か |
| 送信最長辺 | lib/pages.ts `MAX_SIDE` | 2200px | 崩し字の読み取り精度 vs 送信量 |
| JPEG品質 | lib/pages.ts `JPEG_QUALITY` | 0.92 | 同上 |
| サムネ最長辺 | lib/pages.ts `THUMB_SIDE` | 320px | 取り込み一覧の見やすさ |
| スマホの塗り精度 | Redact全体 | — | P6でピンチ拡大・移動を実装済み。指で細かい字を狙えるかは実機で |
| アイコンのみの道具 | ●○↶✕ | — | 職員に伝わるか（ツールチップはPCのみ） |
| カメラ経路 | Intake `capture="environment"` | — | iPhone/Android で直接カメラが開くか |

### 本番（Vercel）の運用

- 本番URL: **https://memo-okoshi.vercel.app**（Hobby・アカウント kanda-houtokukai）。
- **デプロイはCLIから**: `cd ~/memo-okoshi && npx -y vercel@latest deploy --prod --yes`。
  GitHub連携はしていないので **push しただけでは本番に反映されない**（commit→push→deploy の順）。
- Vercel側の環境変数: `GEMINI_API_KEY`（Production/Preview）、`ACCESS_CODE`（Production）。
  値はすべて Vercel 側にだけある。`npx vercel env ls` で一覧。
- [DECISION 2026-09-07] **合言葉ゲート**（middleware.ts）。理由: Hobby では本番ドメインを
  Deployment Protection で守れない（Standard Protection はプレビューのみ・本番保護は Pro の有料枠）。
  `ACCESS_CODE` を設定したときだけ働き、未設定なら素通し。Cookie は SHA-256・30日・HttpOnly。
  未認証の `/api/*` は 401。**合言葉は台帳・リポジトリに書かない**（公開リポジトリ。チャットで伝達済み）。
  変更: `npx vercel env rm ACCESS_CODE production` → `env add` → 再デプロイ。外す: 変数を消して再デプロイ。
- **本番の中身を合言葉なしで確認する手立て**（2026-09-09・P6-b の記録から昇格）: 本番はゲートで画面を
  見られないが、**`_next/` はゲート対象外**なので `https://…/_next/static/css/*.css` を直接取れる。
  手元の `npm run build` が出したCSSと**ハッシュが一致**すれば、少なくとも**CSSは手元と同じもの**が出ている。
  デプロイ後の確認は毎回この手を使う（併せて 307→/gate・`/about` 200・未認証 `POST /api/convert` 401 を見る）。
- ⚠️ **この手立てはCSSにしか効かない**（2026-09-09・P7-b で判明）。**JSのチャンク名は Vercel のビルドと
  手元のビルドで一致しない**（実測: 本番 `main-app-f9b5d20365cb8be2.js` ／ 手元 `main-app-e2b412eb004199cc.js`。
  同名の `webpack-*.js` すら中身が違う）。CSSのハッシュは内容ハッシュなので一致するが、JSは一致しない。
  さらに **`app/page-*.js` の名前は公開ページから辿れない**（`/about` `/gate` の HTML は共有チャンクしか参照しない）。
  → **CSSを触らない回（JSだけの変更）は、外側からは中身を確かめられない。**
  そのときは「デプロイが新しいこと＋ゲート等の応答＋手元のテストとビルド」までを確認し、
  **画面の確認は合言葉を持っている人に頼む**（合言葉は台帳・リポジトリに書かない）。
- **`/about` の変更だけは例外で、本番の実文面をそのまま読める**（ゲートの外に出してあるから）。
  `curl -s https://memookoshi.fknd.jp/about` で確認する（2026-09-09に実施）。
- チーム配下の生URL（`*-kanda-houtokukais-projects.vercel.app`）は Vercel 認証で保護されている。
  外から到達できるのは本番 alias だけ。
- `vercel link` は `.env.local` に `VERCEL_OIDC_TOKEN` 行を追記する（無害・git管理外）。
- **合言葉は 2026-09-07 に入れ替え済み**（旧値が設計チャットに平文で残ったため）。
  台帳・リポジトリ・チャットには書かない。
- ⚠️ **Vercel CLI の `env add` で作成した環境変数は Secret 型になり、ダッシュボードでも
  `vercel env pull` でも値を後から読み出せない**（取得できるのは伏せ字のみ。`env pull` は
  全変数に同じダミー文字列が入る）。`ACCESS_CODE` も `GEMINI_API_KEY` も同じ。
  → **設定した時点で控えるのが唯一の方法**。失った場合は入れ替えるしかない。
  → 合言葉を入れ替えるときは **「新値を設定 → Terminalに表示 → ユーザーが控えたことを確認
    → 再デプロイ」の順**で行うこと（控える前にデプロイして値を失わないため）。

### 独自ドメイン memookoshi.fknd.jp

- fknd.jp の DNS は **エックスドメイン（ns1〜3.xdomain.ne.jp）**。`fukushi-watch.fknd.jp` は
  CNAME → `kanda-houtokukai.github.io`（GitHub Pages）で稼働中。**既存レコードは触らず追加のみ**。
- Vercel 側: `npx vercel domains add memookoshi.fknd.jp memo-okoshi` 済み（2026-09-07）。
  Vercel が要求するレコード: **CNAME `memookoshi` → `cname.vercel-dns.com`**
  （代替: A `memookoshi` → `76.76.21.21`。どちらか一方でよい。サブドメインは CNAME が通例）。
- ユーザー作業（エックスサーバーの管理画面）:
  1. Xserverアカウント → 「ドメイン」→ fknd.jp の「DNS設定」
  2. 「DNSレコード追加」: ホスト名 `memookoshi` ／ 種別 `CNAME` ／ 内容 `cname.vercel-dns.com` ／ TTL は既定
  3. 確認して追加。既存の `fukushi-watch` 行には触れない
  4. 数分〜1時間で反映。Vercel が自動で検証し SSL 証明書を発行する
- 反映後の確認: `dig +short memookoshi.fknd.jp`（cname.vercel-dns.com が返る）→
  `curl -sI https://memookoshi.fknd.jp/`（307 → /gate なら合言葉ゲートが効いている）→
  `curl -s https://memookoshi.fknd.jp/robots.txt`（Disallow: /）。ゲート・noindex は host に依存しない実装。
- **疎通確認 2026-09-07（DNS設定後）— すべて合格**:
  - `dig +short CNAME memookoshi.fknd.jp` → `cname.vercel-dns.com.`（権威 ns1.xdomain でも同じ）
  - Vercel `domains verify` → verified: true。証明書は自動発行（Let's Encrypt・発行まで検証後 約2分・
    期限 2026-12-06 GMT・以後 Vercel が自動更新）
  - `https://memookoshi.fknd.jp/` → 307 → `/gate`（合言葉ゲート有効・ssl_verify=0）／`/gate` 200
  - 未認証 `POST /api/convert` → 401（ゲートがAPIも守っている）
  - `/robots.txt` → `Disallow: /`、`/gate` に `<meta name="robots" content="noindex, nofollow">`
  - **既存 `fukushi-watch.fknd.jp` は無傷**: CNAME → `kanda-houtokukai.github.io.` のまま・https 200
  - 注意: `vercel domains inspect` は CNAME 構成でも「not configured」と警告を出すことがある（A 76.76.21.21
    との比較による既知の見かけ上の警告）。`domains verify` の verified と実際の https 応答を正とする。
  - 注意: 作業した Mac の DNS キャッシュに「レコード無し」が最大1時間残り、ローカルからだけ繋がらない
    ことがある（公開リゾルバ @1.1.1.1 や `curl --resolve` で切り分ける）。
- 本番URL（配布用）: **https://memookoshi.fknd.jp**（memo-okoshi.vercel.app も引き続き有効）

- **devサーバー稼働中に `npm run build` を実行すると `.next` が壊れて500になる**
  （`Cannot find module './xxx.js'`）。ビルド確認はdevを止めてから行うこと。
  壊れたら `rm -rf .next` して起動し直す。
- この自宅Macの環境: `gh` CLIなし（GitHub APIをキーチェーンの認証で直叩き）。
  グローバル設定で `npm install` はdeny（必要時はユーザー承認で一時解除→復元する運用）。
  devサーバー起動エントリは `~/.claude/launch.json` の `memo-okoshi-dev`（port 3210）。

### 実機確認の積み残し（P2〜P4からの宿題）

- iPhone写真形式（HEIC）の取り込み可否（→P2で確認）
- アイコンのみのボタン（⧉/✎）が職員に伝わるか。不可なら初回のみラベル表示（→P4）
- タッチ端末での並べ替え矢印の常時薄表示がうるさくないか（→P4）
- iOS Safari実機確認必須／入力欄16px未満の自動拡大の罠に注意（→P2以降のUI全般）

## いま守る制約（主題別）

> **ここに載っているのは「いま守るべきこと」だけ**。なぜそうしたか（実測値・却下した案・当時の画面の事情）は
> `memo-okoshi-archive-2026-09.md` の「**決定の経緯（台帳から移動）**」に原文で置いてある。
> 新しい決定は**この節の該当する主題へ追記**する。**フェーズ別の節（「P○ の決定」）は作らない**
> （CLAUDE.md「台帳の維持 → B. 決定」）。

### 全体の作法

- **UI仕様の正本は実装**（`app/components/` ＋ `app/globals.css`）。2026-09-07に移管。画面を変えるときは実装を直す。
- `docs/mock/*.html` は**凍結・参照のみ・編集禁止**。実装と食い違ったら実装が正しい。
  memo-okoshi-mock-v6／kuronuri-mock-v1／gate-mock-v1／torikomi-mock-v1／steps-mock-v1／toolbar-mock-v1／tsukaikata-mock-v1。
  ⚠️ **torikomi-mock-v1 の並べ替えだけはモックが誤り**（↑↓）。正しいのは実装の **←→ ＋ ドラッグ**。
- **npm依存を増やさない。** 必要な外部ライブラリは cdnjs から版固定で遅延読み込みする
  （pdf.js 5.4.149／JSZip）。理由: この環境は `npm install` が権限で禁止されていて自走中に承認を得られない。
- テストは `npm test`（Node標準の test runner ＋ 型ストリップ。追加依存なし）。
- **工程名の表示は「伏せる」。コード内の識別子は据え置き**（Step の `"mask"`／`Redact.tsx`／`lib/mask.ts`／
  `kuronuri-mock-v1.html`）。UI文言に「黒塗り」が残らないことは `tests/ui-text.test.mts` が検査する。
  **過去の記録・決定に残る「黒塗り」は当時の記述。書き換えない。**
- **説明文を足さない**（原則4）。アイコンだけのボタンには `aria-label` を付けるにとどめ、見えるラベルは足さない。
  ⚠️ **タッチ環境ではツールチップが出ない**（要否は実機確認で判断する）。
- **ツールチップの向きはCSSの文脈指定だけで切り替える**（JSで測らない・属性も部品も増やさない）。
  下向き＝`header` 内と `.pane-h`／右合わせ＝右端に寄る道具列（`.sec-actions` `.page-acts` `.memo-zoom`
  `.zoom-group` `.sp-item` `.vc-add` `.vc-row`）／右出し＝伏せる画面の左の道具帯（下へ回ったときは上出し）。
- **主ボタンは3状態を塗り分ける**: 押せない＝紙地＋破線／押せるが未完成＝無地のベタ塗り／押せる（`.ready`）＝
  accent＋左肩の色帯。`.tool-btn`（辞書・項目）は先頭に4pxの色帯、ヘッダー右のものは紙色の地。
- **ステップ表示は番号入りのタブ列**・**4状態**（現在地／済んだ（押せる）／**済んだが戻れない**（押せる見た目にしない）／未来）。
  **狭い画面（≤900px）では出さない。**
- ⚠️ **「進めない状態」は画面ごとに意味が違う。ある画面の規則を他へ持ち込まない**（各工程の項を読む）。
- **デザインのモチーフは「紙ファイル×インデックスシール」**。文具トーンの低彩度配色。
  ⚠️ **AIクリシェ配色（紫〜青のグラデーション等）は禁止。** 見出しは Zen Kaku Gothic New。
  賑やかしを足さず、**動きは進捗表現だけに集約する**（完了タブの✓・カウンタの脈動・完成ボタンの呼吸）。
- **開発検証はダミーメモのみ。** 実データは規程整理（P8。旧P7）後まで一切使わない。
- **運用推奨: メモを書く段階からイニシャルにしてもらう**（アプリ側の伏せる工程は保険であって、一次防御ではない）。
- **検索エンジンに載せない**（`app/robots.ts` ＋ metadata.robots の noindex）。

### 工程1 取り込み

- 画面は「机の上に紙を並べる」。**固定ビューポート**（`.ik-root` ＋ `html.lock`。机だけが内側でスクロールする）。
- **経路の出し分け**: iOS/Android＝3経路（カメラ／写真ライブラリ／ファイル）、PC＝ファイル1経路＋D&D。
  **PCにカメラ経路を出さない。** 判定が外れてもファイル経路は常に使える。
- **1回の変換＝複数画像＝1件の記録**（複数枚を1件に統合する）。
- **PDFは端末内でページ画像化**してから伏せる工程へ。**入力形式によらず全ページが伏せる工程を通る。**
- **並べ替えは ←→ ＋ ドラッグ**（ドラッグが主・←→ が副）。複数ページは**渡した順に**AIが読んで1件に統合するので、
  並び順が読み取り内容の正しさに直結する。タッチはつまみ（⠿）からのみ、マウスは紙の本体からも掴める。
  落とし先は純関数 `lib/reorder.ts`、**ドラッグと←→が同じ結果になること**を `tests/reorder.test.mts` が保証する。
- 空きスロットは**机の左上**・紙と同じ **228×314**。後ろに**薄い枠2つ**（≤720px では1つ）を控えさせる。
  薄い枠も**押すと取り込みメニューが開く**（押して何も起きない状態を作らない）が、**読み上げには出さない**
  （`aria-hidden` ＋ `tabIndex=-1`）。
- **紙が1枚以上のときだけ**画面下に操作の帯（左＝枚数・右＝「次へ」）。**0枚なら帯を出さない**（勝手に進ませない）。
  ヘッダーに同じボタンを重ねて置かない。文言は行き先ではなく「次へ」。
- **読み込めないファイルは名前を出してトースト**（黙って捨てない・原則2）。HEIC の変換ライブラリは入れていない。
- 送信画像は **JPEG 品質0.92・最長辺2200px**。

### 工程2 伏せる

- ⚠️ **マスクは元解像度の別レイヤーに描き、送信時に画像へ焼き込む**（`lib/pages.ts` の `exportMasked`）。
  **座標だけ送って元画像を渡す実装は禁止**（原則5）。
- **取り消せない状態を作らない**（消しゴム／戻す／全消し。全消去も undo できる。`tests/mask.test.mts`）。
- ストロークは**画像ピクセル座標**で持つ（拡大率に自動で追従する）。四角も**線と同じ1ストローク**
  （`Stroke.shape="rect"`・対角2点）＝「戻す」が1手で効き、消しゴムで一部を消せる。円・自由図形は作らない。
- 固定ビューポート。ホイール＝拡大（カーソル中心）、ctrl+ホイール/ピンチ＝拡大、トラックパッド2本指＝移動、
  移動モード／中ボタン／Space+ドラッグ＝移動。**「移動」ボタンは全端末で残す**（マウス環境の移動手段）。
- 道具は**画面左の縦帯**。⚠️ **「次へ」は道具の外側に置く**（`.ltool-tools` だけが `overflow-y:auto`）。
  画面が低くても**必ず見える**こと＝出口を隠さない。≤760px では帯が下に回る（`flex-direction:column-reverse`）。
- ⚠️ **塗りが0か所でも進める。**（塗る必要のないメモは実際にある。進めないのは誤り。）
  `tests/p6.test.mts` が「次への表示が `hasPaint` / `strokes.length` / `paintCount` に依存しないこと」を検査する。
- **道具ごとにカーソルを変える**: 移動＝手のひら／ペン＝これから塗る範囲と同じ大きさの輪（塗りつぶし）／
  消しゴム＝同じ輪（輪郭のみ）／四角＝十字。輪は**拡大率と太さに追従**し、**タッチでは出さない**。
- 四角は引いている間だけ枠（白い破線）＋薄い塗りで見せ、**確定するまでマスクに入れない**。
  取りやめは Esc／2本目の指／pointercancel。面積がほぼ無いもの（2px未満）は捨てる。
- ⚠️ **送信前の確認を必ず挟む**（塗りの有無によらず。ここが個人情報の出口だから）。**その回の中身**を書く:
  ①伏せた箇所の数 ②伏せていないページの明示 ③「送るのは伏せたあとの画像です。元の画像はこの端末から出ません。」
  ＋赤帯「伏せ忘れがないか、画面をもう一度見てください。」
  ボタンは左「戻って確認する」・右「変換する」で、**既定の焦点は戻る側**（`Dialog` の `focus:"cancel"`）。
  ⚠️ **「今後表示しない」のような抑制は作らない**（出口の確認を省略させない。テストで見張っている）。

### 項目ライブラリ

- **基本6**（面談概要／本人の発言・様子／家族等の発言／支援者の所感／課題・変化／次回への申し送り）
  **＋追加7**（健康・服薬／生活・住環境／日中活動・就労／金銭管理／リスク・緊急性／関係機関との連携／
  本人の希望・目標）。**自由項目の入力はMVP外。**
- **チェックした構成 → 出力スキーマ → AIプロンプト を動的生成する**（変換前に項目を確定する流れ）。
  `lib/items.ts` の **id が API 契約の正本**（表示文言・タブ・色はモックv6由来）。
- 設定は **localStorage に保存**（個人情報を含まない・端末内）。
- **表示順は定義順と分離**（`ORDER`）。**「次回への申し送り」は締めフラグ**で、追加・復帰した項目は
  **締めの手前に自動挿入**する。カードのホバーで ↑↓ の手動並べ替え（タッチでは薄く常時表示）。**転記も表示順**。
- スキーマに **`record_type` を予約**してある（将来: 面談／電話連絡／会議の記録タイプ拡張。器だけ先に切ってある）。

### 工程3 変換（AIへの依頼）

- **モデル名をハードコードしない。** 一覧取得は維持し、**優先モデルの並び → なければ新しい順**に試す
  （`lib/gemini.ts` の `PREFERRED` / `withPreferred`）。
  [DECISION 2026-08-17 →**条件変更** 2026-09-09。廃止ではない]
  **優先の並びを変えるときはここに日付つきで追記する**（コードを黙って書き換えない）。
- ⚠️ **優先順位は「速さ」ではなく「読めない箇所を黄で申告するか」で決める**
  [DECISION 2026-09-10]。現在の並びは **`["gemini-3.5-flash", "gemini-3.6-flash"]`**
  （`tests/p6.test.mts` が並びを見張る）。並べ替えるときは必ず
  **`docs/samples/dummy-memo-04-hard.png`（わざと5か所を潰した素材）で測ってから**にする。
  誤読を自信ありげに通すモデルは、速くても正確でも**採用しない**。
  - [**追記** 2026-09-10・同日] **試すのは優先リストの中だけ。使えなければ一覧の他のモデルへ流さず、
    正直に止める**（`preferredOnly`）。2026-09-09（P7）の「優先→なければ新しい順」から
    **フォールバックの範囲を限定**した（廃止ではない）。理由: 枠切れの21回目から 3.6-flash に流れると、
    **利用者が気づかないまま「読めない箇所を申告しない」状態に落ちる**。
    静かに品質が落ちるより、使えないと分かるほうが安全。
  - **一覧取得は維持する**（耐更改）。優先リストのモデルが廃止されると候補が空になり、
    「優先モデルが一覧にありません」で止まって気づける。
- ⚠️ **変換できないときは理由を分けて画面に伝える**（`FAIL_TEXT` が文言の正本・`lib/gemini.ts`）:
  **429＝本日の上限（明日また）／503＝混雑（少し待って）／その他**。3つとも
  「**メモと伏せた箇所はそのまま残っています**」で終える（作業が消えたのかが最初の不安だから）。
  APIは 429/503/502 とHTTPも分ける。**生のモデル出力（raw）は画面に出さない。**
  失敗しても取り込んだ紙と伏せた状態は捨てず、伏せる画面に**「もう一度試す」**を出す
  （押すと**送信前の確認を必ず通る**。`tests/p6.test.mts` が見張る）。
- **生成パラメータを明示する**: `temperature: 0`／`response_mime_type: "application/json"`／`maxOutputTokens: 16384`。
  既定値まかせにしない（再現性を優先する、という設計側の判断）。
- **どの項目に入れるかは「何について書かれているか」で決める。「誰が話したか」では決めない**（プロンプト規則9）。
  例: 家族が語った本人の生活の様子は本人についての項目に入れ、語り手は本文中で b（青）として示す。
- **人名（赤）は二重防御**: プロンプト（敬称の有無を問わず・姓のみ/名のみも・**迷っても y でなく r**）＋
  `lib/names.ts` の機械検出。**敬称なしの姓名の機械検出はしない**（誤検知の害が大きい）。
- **低確信（黄）は迷ったら必ず y。** 誤読を自信ありげに通すより、読めないと申告する方がよい。
  発言者・主語の取り違えは「位置関係から判断、迷えば b＋note」。
- **書かれていない項目は空欄。AIは埋めない。**
- **気づきとこぼれは変換と同一APIコールで生成する**（送信回数を増やさない）。
- 変換は30〜40秒。手応えは**ヘッダー直上の3pxプログレスバー＋ボタン文言だけ**（説明文を足さない）。
- **再変換は追加した空の項目だけを埋める**（`mergeReconvert`）。手直し・解決済みマーカーには一切触らない。
  こぼれは二重化させない。**焼き込み済み画像を再送する**（元画像は送らない）。
- 項目の選択は**次の変換から反映**（変換直前に localStorage を読み直す）。

### 工程4 確認

- 黄＝低確信、青＝推定、赤＝人名（対応必須）。
  ⚠️ **赤が残る間は完成も項目コピーも不可**（原則3。`tests/invariants.test.mts`）。黄・青は警告のみで出力できる。
- ⚠️ **黄と青は同じ骨格を持たせる**: ①**そのまま確定（主ボタン・先頭）** → ②選び直す／書き直す → ③消す。
  黄＝「このままで確定する」／青＝「この内容で確定する」。
  入力欄の確定は 黄＝「直して確定」／青＝「書き換えて確定」（主ボタンと紛れる文言にしない）。
  - **③「消す」は青にだけ置く。** 黄はメモに実在する語なので、消せるようにすると原則2（黙って捨てない）に触れる。
  - **赤には①を作らない**（原則3。置き換えるまで完成できない）。誤検知のときは自由入力に同じ語を入れれば
    解消できるので逃げ道はあるが、一手多いのは**意図した摩擦**。
- **黄の候補一覧から、いま表示されている語と同じものは外す**（`candidatesFor`）。主ボタンと重複するため。
  プロンプトが「語彙で確定できなければ cands にこの表記を含める」と指示しているので、実際に重複してくる。
- **編集にAIの再往復はない**（すべて端末内）。
- 文章を直す欄は**内容の量に合わせて伸びる**（入力中も）。**上限 `max-height:60vh`**（保存・取消が画面外へ出ない値）、
  下限 5.5em。`box-sizing:border-box` なので**枠線ぶんを足す**（足さないと最後の行が数px欠ける）。
  手で高さを変えるつまみ（`resize`）は付けない。
- 赤の置換候補は **①アルファベット＋元の敬称（既定）／②「担当」／③自由入力**。
  **同じ名前には常に同じ記号・違う名前には違う記号**（A,B,C…、27人目から AA）。割り当ての単位は1件の記録で、
  **記録に出てくる順**に振る。敬称は引き継ぎ、**表記だけ記録に揃える**（くん→君・さま→様）。
  ⚠️ **敬称が無ければ記号だけ**（「田中」→「A」。「A氏」のように足すと、書かれていない関係性を作ってしまう）。
  まとめ置き換えは①を選んだときだけ（②③はその場かぎり）。
- ⚠️ **人名→記号の対応表は画面の中だけ**（`Review` の state）。**サーバーへ送らない・保存しない**（原則5）。
  `lib/alias.ts` は純関数だけで通信も保存もしない。送信を組み立てる場所（`app/page.tsx`／`app/api/convert`／
  `lib/prompt.ts`）が alias に触れていないことを `tests/invariants.test.mts` と `tests/alias.test.mts` が検査する。
- ⚠️ **こぼれ（拾いきれなかった内容）を黙って捨てない**（原則2）。**移動先は常に選べる**
  ＝並びは「AIの提案 → 表示中の項目 → オフの項目」で、**オフの項目も選べ、選ぶとオンになる**（`＋` を付けて区別）。
  移動先に内容があれば**上書きせず末尾に追記**する。表示中の項目が0件のときはトーストで知らせ、こぼれは保持する。
- **AIの気づきは付箋パネルとして記録と別枠**に置く。全文ベースで項目のオンオフから独立。
  **トーンは観点提示・見落としチェックまで**（支援方針の指示はしない）＋「なぜそう見るか」を一行。
  各付箋に関連項目チップ（押すと該当カードへ。オフ項目は「＋」チップ＝追加導線）。
- ⚠️ **気づき・こぼれを転記テキスト・項目コピーに絶対に混入させない**（原則1。機械テストで保証）。
- **「完成」は右上のまま**（下端の帯にしない）。残件カウンタと隣り合って初めて「なぜ押せないか」が分かるため。

### 工程5 出力

- 選択した項目のテンプレに沿った整形テキストを出す。**画面を閉じたらデータは残らない**（原則5）。
- 全文コピー＋項目ごとコピー（⧉）。**「新しい変換を始める」はコピー成功後にだけ出す**
  （貼り付け前の誤操作で失わないため）。押すと端末内の画像・記録をすべて捨てる。
- **Word は自前の最小 .docx**（JSZip を cdnjs から。npm依存なし）。1項目＝表の1行、余白1.5cm、游ゴシック。
  段落は `<w:p><w:r><w:t>` だけ、**不可視文字とタブは除去**（`tests/p6.test.mts`）。
- **PDF はフォント埋め込みライブラリを使わず、印刷用レイアウト＋ブラウザの印刷**（日本語フォント数MBを毎回読まない）。
- 小見出しの改行は**AI出力の `\n`** を `white-space: pre-line` で表示し、コピー・Word・PDF にも段落として持つ。

### 辞書（事業所の言葉）

- ⚠️ **端末内（localStorage `memo-okoshi:vocab`）にだけ保存**し、変換リクエストに同送してプロンプトへ差し込む。
  サーバーは `sanitizeForPrompt` で絞って使うだけで**保存しない**。
- **分類なしの単純リスト**（語＋任意の意味）。上限 200語・語40字・意味60字。
- **初期セットは入れない**（辞書の価値は「その事業所でしか通じない語」。誤った初期値が組織の表記を上書きする害を避ける）。
  空のときはプロンプトに節を出さない。
- ⚠️ **人名を入れさせない三重の配慮**: ①敬称（さん/様/さま/くん/君/ちゃん/氏）で終わる語は登録拒否
  ②黄からの学習導線は赤（人名検知）には出さない ③プロンプトで「人名は含まれていない語彙」と明示。
  **敬称なしの姓名は機械では見分けられない** → AI委員会の申し合わせ（運用）で補う前提。
- プロンプトの指示は「該当しそうなら優先採用。**ただし書かれていない語を辞書から補わない**」。
- **説明文を足さずに「例」で伝える**: プレースホルダーを具体例に／**空のときだけ**淡い例示3件（「例」札付き）。
  例示は**表示専用**（`lib/vocab.ts` の `VOCAB_EXAMPLES`）で保存もプロンプト送信もしない。
  例示に使うのは福祉現場で一般的な略語のみ（特定の法人・事業所が分かる語は使わない）。
- 学習導線は**黄ポップオーバーの「辞書に追加」チェック**（既定オフ）。
- 見出しは**「事業所の言葉」**。※**プロンプト内の節名「組織の語彙」はAI向けなので据え置き**（`tests/vocab.test.mts`）。
- 入力欄は **`font-size:16px`**（iOS Safari の自動拡大を避ける値）。
- 書き出し／読み込みは JSON 1ファイルに辞書＋項目設定を同梱。読み込みは辞書＝追記（重複は飛ばす・人名ガード適用）、
  項目設定＝置換。
- `tests/vocab.test.mts` が敬称拒否・重複・上限・プロンプト差し込みを検査する。

### 使い方ページ・アイコン・PWA

- `/about` は**作業画面の外の独立ページ**（作業の流れに説明を差し込まない）。入口は全工程のヘッダー・辞書の左で、
  **冊子アイコン＋「使い方」**（≤900px では冊子だけ）。**別タブで開く**（同じタブだと作業中の画像・結果が失われる）。
- ⚠️ **`/about` は合言葉ゲートの外側**（合言葉を渡さずにリンクを共有できることが要件だから）。載るのは仕様と
  運用ルールだけで、**法人名・施設名・個人情報・合言葉を書かない**。noindex は維持。
  ⚠️ **ゲートを開けるのは `middleware.ts` の `PUBLIC`（完全一致の Set）に並べた道だけ。前方一致にしない。**
- **文面の正本は `lib/about-copy.ts`。** `app/about/page.tsx` は器だけで文字を持たない。
  **文の作法**: 一文を短く／当たり前のことは書かない／**すべての文を述語で終える（体言止めを使わない）**／
  **小見出しは名詞句にする（文にしない）**。
  `tests/about.test.mts` が**承認済みの文面（ゴールデン）**と PUBLIC の中身を検査する。1文字でも変わると落ちる。
  ⚠️ **文面や公開する道を変えるときは、設計側の承認を取ってからテストも一緒に直すこと。**
- **PWA は standalone**。**Service Worker・オフライン対応はやらない**（キャッシュを持たないのでデプロイが即反映される）。
- アイコンは**設計側が生成した原本を縮めるだけ**（sips。依存を増やさない）。**原本は加工しない。**
  maskable は宣言しない。**大小で別デザイン**（16・32px＝`memo-okoshi-icon-small-src.png`／
  48・180・192・512px＝`memo-okoshi-icon-src.png`）。**原本2枚の md5 を `tests/icon.test.mts` が控えている**
  （差し替えるときはテストも一緒に直す）。
- `apple-mobile-web-app-capable`（旧名）の meta は `app/layout.tsx` の `<head>` に自分で置いてある（Next は出さない）。

### 行き来と離脱

- 確認 → 伏せる／取り込み: **変換結果は失われ、画像と伏せは残る**（ダイアログあり）。
  伏せる → 取り込み: 失うものなし（ダイアログなし）。ブランド押下＝すべて失う（確認あり）。
  ブラウザの戻る・閉じる: 画像か変換結果があれば `beforeunload`。

## ファイルの地図

| ファイル | 役割 |
|---|---|
| memo-okoshi-handoff.md | 本台帳（正本・引き継ぎの入口はここ1本） |
| CLAUDE.md | セッション起動時の指示・台帳維持規則・凍結指定 |
| docs/mock/memo-okoshi-mock-v6.html | 設計意図の記録（凍結保存・更新しない。正本ではない） |
| docs/samples/dummy-memo-0{1,2}.png | 検証用ダミーメモ（AI生成・個人名なし・別人別日の児童面談） |
| lib/items.ts | 項目ライブラリ（id=API契約の正本／表示文言・タブ・色はモックv6由来） |
| lib/record.ts | 確認画面の状態ロジック（純関数・不変条件はここでテスト） |
| app/globals.css | モックv6の `<style>` の逐語コピー＋末尾に実装用の追加のみ |
| app/components/ | **UI仕様の正本**（Review/SectionCard/Popover/Drawer/OutputOverlay/MemoPane＋Intake/Redact/StepHeader/Toast） |
| lib/mask.ts | 黒塗りのストロークモデル（純関数・undo/全消去の履歴）＋Canvas描画 |
| lib/pages.ts | 取り込み（画像/PDF→ImageBitmap・EXIF向き反映）と焼き込み書き出し `exportMasked` |
| tests/mask.test.mts | 「取り消せない状態にしない」の機械テスト |
| app/robots.ts | 検索回避（noindex） |
| lib/vocab.ts / app/components/VocabDrawer.tsx | 組織語彙の辞書（端末内保存・人名ガード・ドロワー・バックアップの純関数） |
| lib/backup.ts | 辞書＋項目設定の書き出し/読み込み（ブラウザ側） |
| lib/names.ts / tests/names.test.mts | 人名（敬称付き氏名）の機械検出＝赤の保険（原則3） |
| lib/stage.ts / app/components/useZoomPan.ts | 拡大・移動の座標計算とジェスチャー（黒塗り・元メモで共用） |
| app/components/Dialog.tsx | 確認ダイアログ（戻る・やり直し・未塗り・再変換の同意） |
| lib/docx.ts / lib/export.ts | Word（最小 .docx を自前生成・JSZip は CDN）／PDF（印刷用レイアウト＋ブラウザ印刷） |
| tests/p6.test.mts | 再変換マージ・Word の不可視文字/混入検査・バックアップ読み込み |
| docs/mock/kuronuri-mock-v1.html | 黒塗り画面の動作仕様（凍結・参照のみ） |
| docs/mock/torikomi-mock-v1.html | 取り込み画面の動作仕様（凍結・参照のみ。**並べ替えだけは実装が正**） |
| docs/mock/tsukaikata-mock-v1.html | 使い方ページの**体裁**の記録（凍結・参照のみ。**文面の正本は実装へ移管**） |
| docs/mock/steps-mock-v1.html | ステップ表示の比較（凍結・参照のみ。**A案=採用／B案=不採用**） |
| docs/mock/toolbar-mock-v1.html | 操作導線（凍結・参照のみ。下の操作帯／左の縦帯／使い方は**案C採用**） |
| lib/about-copy.ts | 使い方ページの**文面の正本**（承認済みの文面。tests/about.test.mts が控えと突き合わせる） |
| docs/assets/memo-okoshi-icon-src.png | アプリアイコンの原本（**無加工**。48px以上のもと） |
| docs/assets/memo-okoshi-icon-small-src.png | 小サイズ版の原本（**無加工**。16・32px のもと。橙地で潰れない） |
| app/about/ ＋ public/icon-*.png ＋ app/manifest.ts | 使い方ページ／アイコン各サイズ／マニフェスト（standalone） |
| tests/about.test.mts / tests/icon.test.mts | 使い方ページの文面とゲートの穴／アイコンとマニフェストの検査 |
| lib/reorder.ts / tests/reorder.test.mts | ページ並べ替えの純関数（落とし先の判定・ドラッグと←→の一致） |
| lib/alias.ts / tests/alias.test.mts | 人名の置き換え記号（同じ名前＝同じ記号・敬称の引き継ぎ。**画面内だけ**） |
| docs/mock/gate-mock-v1.html | 合言葉画面の動作仕様（凍結・参照のみ） |
| docs/samples/dummy-memo-03-names.png | 架空の氏名を書き込んだ検証用（赤マーカーの検証。PIL で生成） |
| docs/samples/dummy-memo-04-hard.{png,md} | **判読しにくい**検証用（成人・就労/金銭管理。**わざと5か所を潰してある**。md が原本＝正解） |
| docs/samples/make-dummy-04-hard.py | 上を作る一回きりのツール（PIL＋Klee。種を固定。ビルド・テストとは無関係） |
| tests/vocab.test.mts | 辞書とプロンプト差し込みのテスト |
| tests/ui-text.test.mts | 画面文言の機械検査（「黒塗り」が残っていないこと・辞書の例示が正しい語であること） |
| middleware.ts / lib/gate.ts | 合言葉ゲート（ACCESS_CODE 設定時のみ・Cookie照合） |
| app/gate/page.tsx / app/api/gate/route.ts | 合言葉の入口と照合API |
| tests/invariants.test.mts | 不変条件2点の機械テスト（`npm test`） |
| public/dev-fixture.json | `?fixture=1` 用の開発データ（モックv6の内容を再現） |
| lib/prompt.ts | 変換プロンプト生成（項目構成を実行時差し込み） |
| lib/gemini.ts | Geminiクライアント（一覧取得→**優先モデル→なければ新しい順**・temperature 0・キーはヘッダ送信） |
| app/api/convert/route.ts | 変換API（multipart受け→1コール→JSON検証・救済パース） |
| app/page.tsx | 画面の状態機械（取り込み→黒塗り→変換→確認→新しい変換）。元画像はここのメモリにしか無い |
| .env.local | GEMINI_API_KEY（**git管理外**・コミット禁止検証済み） |

同種ファイルの正本: UI仕様は**実装（app/components + app/globals.css）が正本**（2026-09-07移管）。
モックv6は設計意図の記録として凍結保存。旧版モック（v1〜v5）はリポジトリに置かない。

## ロードマップ

- **P0: 器の整備** ✅（2026-08-17 完了）
- **P1: 変換の核** ✅（2026-08-18 完了）ダミー画像→構造化JSON（動的スキーマ・黄青赤スパン・気づき・こぼれ）の疎通
- **P2: 取り込み＋黒塗りUI** ✅（2026-09-07 完了）3経路・複数・PDFページ化・焼き込み送信
- **P3: 確認・出力UI** ✅（2026-09-07 完了）モックv6の本実装移植
- **P4: Vercelデプロイ** ✅（2026-09-07 完了）https://memo-okoshi.vercel.app ・実機確認は次の一手
- **P5: 組織語彙の辞書＋独自ドメイン** ✅（2026-09-07 完了・memookoshi.fknd.jp 疎通確認済み）
- **P6: 実機フィードバックによる改良** ✅（2026-09-08 完了・Windows 実機確認は積み残し）
- **P6-b: 文言とUIの細部** ✅（2026-09-09 完了）工程名「伏せる」・ツールチップの向き・主ボタンの3状態・辞書の例示
- **P6-c: 取り込み画面の作り直し** ✅（2026-09-09 完了）机と紙・ドラッグ並べ替え・←→
- **P6-d: 使い方ページとアプリアイコン** ✅（2026-09-09 完了）/about（ゲート外）・アイコン・PWA(standalone)
- **P6-e: ステップ表示と取り込み配置** ✅（2026-09-09 完了）番号入りのタブ列（A案）・空きスロットを左上へ
- **P6-f: 複数枚に気づける取り込み** ✅（2026-09-09 完了）＋の後ろに薄い枠を2つ
- **P6-g: 人名の置き換え記号** ✅（2026-09-09 完了）アルファベット＋敬称・同じ名前は同じ記号
- **P6-h: 編集欄で全文が見える** ✅（2026-09-09 完了）内容に合わせて伸びる・上限60vh
- **P6-i: 操作導線の作り直し** ✅（2026-09-09 完了）取り込みは下の帯・伏せるは左の縦帯・使い方は案C
- **P6-j: 四角で範囲をまとめて伏せる** ✅（2026-09-09 完了）
- **P6-k: 次への不具合と送信前の確認** ✅（2026-09-09 完了）
- **P7: 振り分けの再現性とこぼれの移動先** ✅（2026-09-09 完了）優先モデル＋温度0＋振り分け基準・移動先を常に選べる
- **P7-b: 黄マーカーに「このままでよい」** ✅（2026-09-09 完了）黄と青の骨格を揃える・候補から現在の語を外す
- **P7-c: 読み取り精度のためのモデル比較** ✅（2026-09-10 完了）優先を 3.5-flash へ・判読しにくい検証素材を追加
- **P7-d: 枠切れ時のフォールバックを止める** ✅（2026-09-10 完了）優先リストの外へ流さない・429と503を区別して伝える
- **P8: 現場試用（AI委員会）・規程整理**（Gemini有料設定の切り替えはここで判断）
  ※ 2026-09-09に繰り下げ。設計側から出た指示書「P7」が別内容（上記）だったため、旧「P7: 現場試用・規程整理」を
    **P8 に改番**した。以前の記録に出てくる「P7（規程整理）」はこの P8 を指す。
- 将来枠（設計意図と原則との関係。**原則を知らずに実装しないための記録**）:
  - **訂正の学習**（次の有力候補）: 黄マーカーの修正内容を端末内に蓄積し、同じ誤読を次回候補の先頭に
    出す。P5の辞書と同じ基盤（localStorage＋プロンプト差し込み）に載る。端末内で閉じるので原則5と
    両立する。蓄積するのは「誤読→正しい語」の対だけにし、文脈（前後の文）は保存しない。
  - **過去記録との照合**: 前回の申し送りと今回のメモを突き合わせ、宿題の抜けを指摘する。
    **記録の保存を前提とするため原則5（保存しない変換器）と正面衝突する**。実装するなら
    端末内保存に限る設計＋規程整理が必須。現時点では構想止まり。**勝手に着手しない**。
  - **記録タイプの拡張**: 面談／電話・連絡調整／会議メモ。`record_type` の予約枠は確保済み
    （スキーマ・API・状態に既にある）。項目ライブラリをタイプ別に持つ設計になる。
  - **個人アカウント／Googleログインへの移行**: 実運用で実データを扱う段階では「誰がいつ変換したか」の
    記録が規程上ほぼ必須になる。合言葉ゲートでは足りなくなる。ログを持つこと自体が原則5の
    「保存しない」の範囲をどこまで含むか（画像・記録は保存せず操作ログだけ持つ等）を規程側で決めてから。
  - 自由項目／部分再変換オプション（従来からの将来枠）

---

# 決定事項リスト（2026-08-17確定）→ アーカイブへ移動

> 2026-09-09、内容を上の **「いま守る制約（主題別）」** へ畳んだうえで、原文を
> `memo-okoshi-archive-2026-09.md` の「決定の経緯（台帳から移動）」へ移した。
> **「正本」を名乗るものを2つ置かないため**（守るべきことの正本は「いま守る制約」ひとつ）。
> 当時の合意そのものを読みたいときだけアーカイブを開くこと。

# 記録（直近10件まで。超えたら古い順に memo-okoshi-archive-YYYY-MM.md へ原文移動）

## 2026-09-09 P6-g: 人名の置き換え記号（自走）

- 赤の候補を「アルファベット＋元の敬称」→「担当」→自由入力の順に。記号は `lib/alias.ts`（純関数）が決め、
  対応表は Review の state にだけ持つ。記録に出てくる順に A,B,C… を振り、同じ名前には常に同じ記号を返す。
- アルファベットの候補を選んだときだけ、同じ名前の未解決の赤をまとめて置き換える（`resolveRedWhere`）。
- 検証（フィクスチャに赤を一時追加して実施・確認後に原状復帰）: 田中→「A」・そうたくん→「B君」・
  山田先生→「C先生」と候補が出る／そうたくんを押すと2か所が同時に B君 になり人名カウンタ 4→2・
  トースト「人名を置き換えました（2か所）」／「担当」と自由入力は押した1か所だけ（4→3→2）。
- テスト 61→70件（alias.test.mts と不変条件1件を追加）。`npm run build` 通過。本番デプロイまで実施。

## 2026-09-09 P6-h: 編集欄で全文が見える（自走）

- 文章を直す欄（`SectionCard` の textarea）を内容の量に合わせて伸ばした。開いた直後は `useLayoutEffect` で
  描画前に、入力中は `onChange` で測り直す。窓の幅が変わると折り返しも変わるので resize でも測り直す。
- 検証（実測）: 短い内容は下限の84px（5.5em）でスクロールなし／3行→181px・6行→340px でスクロールなし／
  10行→540px（＝60vh の上限）で中がスクロール／20行でも540pxのまま。6行を保存して開き直しても340pxで全文が見える。
- 途中、`box-sizing:border-box` のため枠線ぶん（3px）が足りず最後の行が欠けていたのを実測で見つけて直した。
- 台帳の記録が11件目になるため 2026-09-07 P2 をアーカイブへ原文のまま移動（点検済み）。
- テスト 70→71件。`npm run build` 通過。本番デプロイまで実施。

## 2026-09-09 P6-i: 操作導線の作り直し（自走）

- 手順0: `toolbar-mock-v1.html` を docs/mock へ凍結配置（md5一致）。使い方の入口は**案C採用**を台帳へ。
- 1: 取り込みに下の操作帯（枚数＋「次へ」）。ヘッダーの「伏せるへ」は撤去。0枚では帯を出さない。
- 2: 伏せるの道具を左の縦帯へ。3群＋区切り線＋一番下に「次へ」。旧・下部ツールバーは削除。
  狭い画面は `column-reverse` で下へ回し、「次へ」は sticky で右端に固定。
  検証中、狭い画面では道具が横スクロールして「次へ」が画面外（x=575）に隠れることに気づき、sticky を足した。
- 3: 使い方の入口を冊子＋文字（案C）に。狭い画面では文字を隠す。
- ツールチップは左帯では右側に出す（中央合わせだと画面外へ出るため）。
- 検証: 取り込み0枚で帯なし／2枚で「2枚」＋「次へ」／伏せるPCは左帯（幅64px・次へは最下部 y=762）／
  スマホ390pxでは下帯・次へは右端に貼り付いて常に見える（x=315〜380）／ツールチップが右に出る。
- 確認事項: 確認画面の「完成」は右上のまま（理由は「P6-i の決定」に記載）。
- テスト 71→73件。`npm run build` 通過。本番デプロイまで実施。

## 2026-09-09 P6-j: 四角で範囲をまとめて伏せる（自走）

- `lib/mask.ts` の Stroke に `shape:"rect"` を足し、`rectOf()`（対角2点→左上と大きさ）と
  `drawStroke` の塗り分けを追加。線と同じ1ストロークなので undo・消しゴム・焼き込みはそのまま効く。
- `useZoomPan` の `onDrawEnd` に「中断された」を渡すようにし、ピンチに移ったときは四角を捨てる。
  Esc でも取りやめる。引いている間は view canvas に枠＋薄い塗りを重ねるだけ（マスクには入れない）。
- 道具帯に「四角」を追加。四角のときは塗る範囲の輪を出さず十字カーソルに戻す。
- 検証（合成ポインタ＋画素検査）: 全体表示で1つ・Escで1つ取りやめ・拡大中に1つ引いてから送信直前の
  画像を横取り。**黒い塊はちょうど2つ**（Escの分は残っていない）、中心と内側の角が [0,0,0]、
  塗っていない場所は元の [239,233,219]。大きさは 498×206 と 238×90 で、画面上の寸法÷倍率と一致。
- 台帳の記録が11件目になるため 2026-09-07 P5 をアーカイブへ原文のまま移動（点検済み）。
- テスト 73→76件（mask.test.mts に四角の3件）。`npm run build` 通過。本番デプロイまで実施。

## 2026-09-09 P6-k: 次への不具合と送信前の確認（自走）

- 不具合1（塗りが無いと進めない）: 「次へ」自体は無条件に描いていたが、**塗る必要がないメモでも進める**
  という仕様を明文化し、表示が塗りの状態に依存しないことをテストで固定した。あわせて「進めない状態」の
  意味が画面ごとに違うことを台帳へ（取り込み=0枚なら進めない／伏せる=0か所でも進める／確認=赤が残る間は不可）。
- 不具合2（低い画面で押し出される）: 実測で 600px のとき「次へ」が y=643〜691 と画面外にあり、最後の道具
  （移動）も下端 633 で切れていた。道具を `.ltool-tools`（`flex:1;min-height:0;overflow-y:auto`）で包み、
  「次へ」をその外側へ出して解決。1000/800/700/600/500px で「次へ」も全道具も到達可能なことを実測。
- 送信前の確認: `tryConvert` を「必ずダイアログを出す」形に変え、伏せた箇所の数・伏せていないページ・
  送るのは伏せたあとの画像であることを行ごとに出す。ボタンは左が「戻って確認する」で既定の焦点もそちら。
  `Dialog` に `focus` を足した（既定は従来どおり進む側）。
- 点検: 確認画面の「完成」はヘッダーが sticky で 500px でも常に見える／取り込みの帯は 400px でも見える。
- テスト 76→79件（次へが塗りに依存しない・低い画面でも届く・確認が必ず出て抑制機能が無い）。
  `npm run build` 通過。本番デプロイまで実施。

## 2026-09-09 P7: 振り分けの再現性とこぼれの移動先（自走）

- 手順0: 記録が10件に達していたため、最も古い **2026-09-09 P6-b** を `memo-okoshi-archive-2026-09.md` へ
  **原文のまま**移した。移す前に点検し、「本番の中身を合言葉なしで確認する手立て（CSSのハッシュ一致）」を
  「本番（Vercel）の運用」へ昇格。ツールチップの向き・主ボタンの3状態・辞書の例示は「P6-b の決定」節として
  台帳に残っているので昇格不要と判断した。
- 1: **原因を先に実測で確かめた。** ダミーメモ01（`docs/samples/dummy-memo-01.png` を 2200px/JPEG92 に
  変換したもの）で同じ変換を5回。当たったモデルが **3.6 / 3.7 / 3.6 / 3.6 / 3.5** と3種類に散らばり、
  上位モデルが **HTTP 503（混雑）** で落ちるたびに別のモデルへ流れていた。そのたびに
  「本人の発言・様子」の中身が家庭での様子と園での様子で入れ替わっていた（3回 対 2回）。
- 1: 対処は3つ。① `lib/gemini.ts` に **`PREFERRED = ["gemini-3.6-flash","gemini-3.5-flash"]`** を置き、
  `withPreferred()` で一覧の先頭へ並べ替える（**一覧取得は維持・ハードコードで固定はしない**）。
  ② `generationConfig` に **`temperature: 0`** を明示（`response_mime_type` と `maxOutputTokens` も並べて書いた）。
  ③ `lib/prompt.ts` に規則9「**同じ内容を2つ以上の項目に重ねて書かない。どの項目に入れるかは
  『何について書かれているか』で決める（『誰が話したか』では決めない）**」を追加。
  **①②だけでは家庭／園の割れが残り、③で初めて 5/5 一致になった**（判断基準を文章で与える必要があった）。
- 1: 改善後の実測（同じ画像で5回）: モデルは **5回とも 3.6-flash**、「本人の発言・様子」は
  **5/5 とも家庭での様子**（330±7字）、概要・課題・所感・申し送りも 5/5 同じ項目に落ちた。
  数値の表は「P7 の決定」節に置いた。**黄の件数だけは 0/2/1/2/0 と揺れたままで、未解決の課題に残した**
  （モデル差ではなく生成そのもののばらつきだと切り分けられたのが今回の収穫）。
- 2: こぼれの行に **`⌄`（他の項目へ移す）** を足し、押すと移動先の一覧が出る形に。
  並びは **提案 → 表示中の項目 → オフの項目**で、オフの項目は頭に `＋` を付けて出し、選ぶとその項目がオンになる。
  部品は増やさず `Popover` に `mode:"picker"` の分岐を足しただけ（`options` に `off` を持たせた）。
  **説明テキストは足していない。** 検証（`?fixture=1`・1240×1450）: 提案「健康・服薬」がオフでも先頭に出て、
  一覧は **13件**（＋健康・服薬／面談概要／本人の発言・様子／家族等の発言／支援者の所感／課題・変化／
  次回への申し送り／＋生活・住環境／＋日中活動・就労／＋金銭管理／＋リスク・緊急性／＋関係機関との連携／
  ＋本人の希望・目標）。狭い画面（395×709）でも一覧は本文の高さを押し広げるので、スクロールで最後の項目まで届く。
- ロードマップの旧「P7: 現場試用・規程整理」は **P8 に改番**した（指示書の「P7」が別内容だったため）。
- テスト 79→82件。`npm run build` 通過（devを止めてから実行）。本番デプロイまで実施（commit `%COMMIT%`）。

## 2026-09-09 台帳の整理（決定の節が無制限に増える構造を直した）

- きっかけ: P7の報告で「台帳46,085文字。増えているのは記録ではなく決定の節」と挙げた点。
  CLAUDE.md の維持規則が**記録の件数しか見ていなかった**ため、「P6-b の決定」「P7 の決定」のような
  **フェーズ別の節が件数制限の外で無限に積み上がる**構造になっていた（決定の節だけで14,925文字）。
- 1: CLAUDE.md の「台帳の維持」を **A（記録）／B（決定）／C（いま効いている決定を見えなくしない）** の
  3節に組み直した。Bで**フェーズ別の節を禁止**し、置き場を `## いま守る制約（主題別）` ひとつに定めた。
  分量の目安は**制約1.2万文字**。超えたときは ①同じ主題をまとめる ②経緯をアーカイブへ ③主題ごと移す、の順。
  Cに**制約と経緯の見分け表**（①コードを縛るか ②破ったら何が起きるか ③いつの話か）を置き、
  **①が「縛る」なら②③に関わらず残す／迷ったら残す**と決めた。
  テストが見張っている制約は1行＋テスト名でよい／**テストが無い制約は本文のまま残す**も明記。
- 2: ⚠️ **点検で分かったこと: 「不可侵の設計原則5点」が台帳のどこにも書かれていなかった。**
  指示書の中にしかなく、台帳には「原則3」「原則5」という**番号での参照だけ**があった。
  過去の指示書から原文を回収し、**台帳の最上段**（現在地より前）に置いた。整理の対象外と CLAUDE.md にも明記。
- 2: フェーズ別の12節＋「原則・仕様」「組織語彙の辞書（P5）」を `memo-okoshi-archive-2026-09.md` の
  **「決定の経緯（台帳から移動）」へ原文のまま**移し（20,960文字）、結論だけを主題別に畳み直した
  `## いま守る制約（主題別）`（全体の作法／工程1〜5／項目ライブラリ／辞書／使い方ページ／行き来と離脱）を作った。
- 2: 「決定事項リスト（正本・2026-08-17確定）」も**アーカイブへ移した**。**「正本」を名乗るものが2つあると
  どちらを守ればよいか分からなくなる**ため。移す前に、畳み残していた**項目ライブラリ・AIの気づきのトーン・
  デザインのモチーフ（AIクリシェ配色の禁止）・record_type の予約・運用推奨（メモ段階からイニシャル）・
  予備Claude API** を制約側／注意事項側へ拾い上げた（1行ずつ突き合わせて確認）。
- 2: 「原則・仕様」に混ざっていた**未実装・保留**（位置連動のフック、iOSのホーム画面アプリと Safari の
  保存領域の分断、HEIC、Gemini無料枠の規約、ダミー素材の限界）を
  「生きている注意事項 → 未実装・保留（触るときに読む）」として独立させた。
  同名だった見出し「実機確認の積み残し」は片方を「（P2〜P4からの宿題）」に改めた。
- 結果: 台帳 46,085 → **約3.45万文字**（−25%）。うち「いま守る制約」は **9,311文字**（目安1.2万以内）。
  ついでに「現在地」の各回の羅列（ロードマップと重複）を落とし、本番の確認手順とデプロイ状態に絞った。
  全体の点検値は 3.2万→**4.0万文字**に上げた。整理後の定常が約3.5万で、3.2万では毎回鳴って意味を持たないため
  （内訳は CLAUDE.md に書いた）。
  記録は10件だったので最古の P6-c をアーカイブへ原文のまま移してからこの記録を足した。
- 3: 自己点検（CLAUDE.md C の最後の手順）。**台帳だけを読んで**①現在地 ②不可侵の原則5点
  ③いま守る制約 ④生きている注意事項 ⑤次の一手 がすべて読み取れることを確認した。
  移した節への参照（「決定の経緯」）も台帳側に明記してあり、経緯が必要なときの行き先が分かる。
- コードは1行も変えていない。`npm test` 82件通過（回帰がないことの確認）。

## 2026-09-09 P7-b: 黄マーカーに「このままでよい」を足す（自走）

- 設計側の指摘: 黄のポップオーバーに**「表示されているままで正しい」を選ぶ手段がなかった**。
  候補から選ぶか自由入力するかしかなく、**最も多いはずの結末（読みは合っていた）に出口が無かった**。
- 直し方は「文言を1つ足す」ではなく**黄と青の骨格を揃える**とした。青は
  ①この内容で確定する（主ボタン）②書き換えて確定 ③この推定は削除する、と3つの結末を持っていたのに、
  黄は②相当しか無かった。黄に①を主ボタンとして先頭に置き、**青と同じ位置・同じ調子**の
  「**このままで確定する**」にした。押すと `resolveToken(..., null)` で**語を変えずに**解決済みにする
  （他の候補を選んだときと同じ扱い。青の「この内容で確定する」と同じ道）。
- 入力欄の確定を「この内容で確定」→**「直して確定」**に改めた。主ボタン「このままで確定する」と
  紛らわしかったため（青の「書き換えて確定」と揃う）。自由入力そのものは現行どおり。
- 候補の一覧から**いま表示されている語を外す**純関数 `candidatesFor` を `lib/record.ts` に置いた。
  `lib/prompt.ts` が「語彙で確定できなければ cands にこの表記を含める」と指示しているので、**実際に重複する**
  （開発フィクスチャも `s:"作業所" / cands:["作業所","作業書"]`）。外した結果、候補は「作業書」だけになった。
- 辞書に追加のチェックはそのまま効く（`addEntry(..., { term: val ?? tk.s })` が null のとき現在の語を使う）。
- 点検（指示どおり青・赤も確認した）:
  - **青は欠落なし。** 採用・書き換え・削除の3つが揃っている。
  - **黄に「削除する」は作らない**と判断した。黄はメモに実在する語で、消せるようにすると
    原則2（黙って捨てない）に触れるため。青の推定はメモに無い内容なので削除があってよい。
  - **赤は「そのまま」が無いのが正しい**（原則3）。ただし誤検知のときは**自由入力に同じ語を入れれば解消できる**
    ＝逃げ道はあり、一手多いだけ。緩めずそのままにした。
- 実機確認（`?fixture=1`・1240×1000）: ポップオーバーは
  「読み取りに自信なし／**このままで確定する**（主）／作業書／自分で入力して直す／直して確定／辞書に追加」。
  押すと **要確認の読取 1→0**、語は「作業所」のまま、黄の網かけが消えて「本人の発言・様子」に ✓ が付いた。
  トーストは「確定しました」。
- **追補（同日・設計側の指示で実施）**: `/about` の黄の説明を差し替えた。
  旧「読み取れなかった箇所です。候補から選ぶか、自分で直してください。」
  → 新「**読み取れなかった箇所です。そのままでよければ確定し、違っていれば候補から選ぶか自分で直してください。**」
  `lib/about-copy.ts`（文面の正本）と `tests/about.test.mts` のゴールデンを一緒に直した。
  文の作法の検査（体言止めなし・述語で終わる・一文60字以内）も通っている（2文・12字と36字）。
  `docs/mock/tsukaikata-mock-v1.html` は**凍結なので触っていない**（食い違ったら実装が正しい）。
  表示の確認: 900px で2行・390px で3行に折り返し、横あふれなし（`scrollWidth - innerWidth = 0`）。
- 追補の点検（青・赤の説明）で**青に軽い不足・赤にもう一段の不足**が見つかり、報告のうえ
  **2026-09-10に設計側の指示で両方とも差し替えた**:
  - 青 旧「使うかどうかを判断してください。」（採用と削除しか指さず、書き換えて使う道が読み取れない）
    → 新「メモにない内容を前後から推測した箇所です。**そのままでよければ確定し、違っていれば書き換えるか
    削除してください。**」
  - 赤 旧「置き換えるまで完成できません。」（制約だけで手段を書いていない）
    → 新「人名かもしれない語です。**候補から選ぶか自分で入力して、置き換えてください。**置き換えるまで
    完成できません。」
  - [DECISION 2026-09-10] **赤だけ3文になるのを許容する**。**強制を伴う唯一の印**なので、
    手段（どう置き換えるか）と制約（置き換えるまで完成できない）の**両方を伝える必要がある**。
    他の印は2文のままにする。
  - これで**3つの印がすべて「①そのまま／①どう直すか → ②制約」の同じ形**になった
    （実装側の「黄と青は同じ骨格を持たせる」と説明側が揃った）。
- ⚠️ **デプロイ後の確認で分かったこと**: これまで使ってきた「CSSのハッシュ一致で本番＝手元ビルドを確かめる」手は
  **CSSにしか効かない**。P7-b はCSSを触っていないのでハッシュは当然一致するが、それは何も証明しない。
  JSで確かめようとしたが、**Vercelのビルドと手元のビルドではチャンク名が違い**（本番 `main-app-f9b5d20365cb8be2.js`
  ／手元 `main-app-e2b412eb004199cc.js`。同名の `webpack-*.js` も中身が別物）、
  さらに `app/page-*.js` の名前は公開ページの HTML から辿れなかった（共有チャンクしか参照していない）。
  `vercel inspect` にも git のコミット情報が付かない（CLIデプロイのため）。
  → **CSSを触らない回は外側から中身を確かめられない**ことを「本番（Vercel）の運用」に⚠️付きで書いた。
  今回確認できたのは「デプロイが新しいこと・`/` 307→/gate・`/gate` 200・`/about` 200・
  未認証 `POST /api/convert` 401・robots は Disallow」まで。**画面の確認は設計側に依頼する。**
- テスト 82→86件。`npm run build` 通過（devを止めてから実行）。本番デプロイまで実施（実装の commit `0abaae9`）。

## 2026-09-10 P7-c: 読み取り精度のためのモデル比較（自走）

- 背景（設計側）: 実機で本物の走り書きを試したところ、整った字で85〜90%、達筆・文脈推論が要る字で急落。
  速度重視の軽量モデルより上位モデルのほうが読めるのではないか、という問い。
- 0: **測る素材を先に作った。** 手持ちのダミー3枚はどれも児童面談で字が整いすぎている。
  成人（就労・金銭管理）の題材で、**わざと5か所を人が見ても読めないほど潰した**
  `docs/samples/dummy-memo-04-hard.png` を作った（原本＝正解は同名の .md、生成ツールは .py・種を固定）。
  潰したのは **眠剤／週1〜2／訪看／12,000円／10/3**。**ここを黄で申告できるか**が最重要の観点。
- 1: 一覧取得で generateContent 対応は40件。画像で使えるものを当たった結果、**選択肢は実質 flash 系だけ**だった:
  - ⚠️ **Pro系は無料枠では呼べない**（2トークンの ping でも 429・入力トークンの日次枠）。
  - `gemini-2.5-pro` は 404（新規提供終了）、`gemini-3.5-transcribe` は画像入力に非対応、
    `gemini-flash-latest` は最新 flash と同じ枠の別名。
  - **`thinkingConfig.thinkingBudget` は 3.6/3.8-flash で受け付ける**が、3.5-flash では
    指定しても出力もトークン数も変わらなかった（既定の思考で足りている）。
- 2: 同じ画像で3回ずつ（判読しにくい素材）:

  | モデル | n | 秒 | 黄の件数 | 青の件数 | 潰した5か所の扱い |
  |---|---|---|---|---|---|
  | 3.6-flash（当時の先頭） | 2 | 15.6 | 1 / 0 | 2 / 5 | ⚠️ **「訪看」を「姉から声かけ」と断定**（2回とも・申告なし）。「眠剤」は黙って落とす |
  | **3.5-flash** | 3 | 24.8 | **2 / 2 / 4** | 8 / 8 / 6 | ✅ **5か所すべて正しく読み、黄または青で申告** |
  | 3.5-flash＋think8k | 3 | 20.9 | 4 / 4 / 2 | 6 / 6 / 8 | 3.5-flash と同じ（思考枠の指定は効かない） |
  | 3-flash-preview | 3 | 16.5 | 1 / 1 / 2 | 5 / 5 / 3 | 「訪看」を「訪問者」と誤読するが**毎回黄で申告**した |

  ⚠️ **3.6-flash の誤りは「読めなかった」ではなく「書かれていない事実を作った」**（訪問看護→姉）。
  黄0件の回もあり、**設計の核（読めない箇所を申告させる）に照らして不可**と判断した。
- 3: **推奨＝`gemini-3.5-flash`。** 判断基準の1番（申告するか）と2番（正しさ）で明確に上。
  3番（再現性）も下記のとおり良い。4番（時間）だけ劣るが 24〜31秒で許容内。
  → `PREFERRED` を **`["gemini-3.5-flash", "gemini-3.6-flash"]`** に入れ替えた（ハードコードはしない構造のまま）。
  `tests/p6.test.mts` に並びを見張るテストを足した（速さで黙って並べ替えられないように）。
- 4: **再現性の再検証**（P7と同じ手順・ダミーメモ01で5回）:
  **6項目すべてが5/5で埋まり**（3.6-flash は「家族等の発言」が3/5だった）、
  「本人の発言・様子」は**5/5とも家庭での様子**、出力の異なりは**5回で2通り**だけ。
  黄は **2/2/2/4/4** で、**0件の回が無い**（3.6-flash は 0/2/1/2/0 で0件が2回あった）。
  → **揺れは増えていない。むしろ減った。** 所要は 24〜31秒（3.6-flash は 15〜20秒）。
- ⚠️ 計測中に分かった**無料枠の上限**（1日20リクエスト／モデル・503の失敗も枠を消費）は
  「生きている注意事項」に独立させた。**21回目から別モデルに流れる＝P7の再現性が崩れる**ため、
  P8（有料切り替え）の判断材料になる。
- 生成パラメータは `temperature: 0` のまま維持。3.5-flash でも挙動は変わらなかった。
- テスト 86→87件。`npm run build` 通過。本番デプロイまで実施（実装の commit `3457123`）。

## 2026-09-10 P7-d: 枠切れ時のフォールバックを止める（自走）

- 設計側の指摘: 無料枠は1日20リクエスト/モデル。枠切れの21回目から 3.6-flash に流れるが、
  そのモデルは **P7-c の実測で「書かれていない事実を作る」**（訪問看護の声かけを「姉から声かけ」と断定）
  と分かっている。つまり**枠切れに気づかないまま品質が落ちる**構造だった。
- 方針は「使えないときは、別のモデルに流さず正直に止める」。
  [DECISION 2026-09-10] **フォールバックの範囲を優先リストの中だけに限定**した
  （2026-09-09の「優先→なければ新しい順」の**範囲の変更**。廃止ではない）。
  `withPreferred`（優先＋一覧の残り）を **`preferredOnly`（優先リストにあるものだけ）** に置き換えた。
  先頭が駄目なら2番目までは試す（どちらも P7-c の基準を満たすと確認済み）。
- **一覧取得は維持**（耐更改）。優先リストのモデルが廃止されると候補が空になり、
  「優先モデルが一覧にありません（PREFERRED の見直しが必要です）」で止まって気づける。
  一覧はプロセス内でキャッシュし、全滅したら捨てる（増減を次回に拾えるように）。
- [DECISION 2026-09-10] **429（枠切れ）と 503（混雑）を区別して伝える。** 文言の正本は
  `lib/gemini.ts` の `FAIL_TEXT`、理由の判定は `reasonOf`（**1つでも503が混じれば「待てば直るかも」を優先**）:
  - 枠切れ「本日の利用上限に達しました。明日また試してください。」
  - 混雑「いま混み合っています。少し待ってから、もう一度試してください。」
  - 3つとも「**メモと伏せた箇所はそのまま残っています。**」で終える（作業が消えたのかが最初の不安）。
  APIも 429/503/502 とHTTPを分ける。**生のモデル出力（raw）を画面に出すのはやめた**（読めない情報を見せない）。
- [DECISION 2026-09-10] **再試行の導線はエラー欄の中に置く**（「もう一度試す」）。
  押すと `tryConvert` を通るので、**送信前の確認（P6-k）は必ず挟まる**。抜け道を作らない。
- 実機確認（`?` なしの通常経路・1240×900・`/api/convert` だけ差し替えて 429/503 を再現）:
  - 429 → 「本日の利用上限に達しました。…」＋「もう一度試す」。伏せる画面のまま、紙も塗りも残る。
  - 503 → 「いま混み合っています。…」に変わる。
  - 「もう一度試す」を押すと**送信前の確認ダイアログが出る**ことを確認した。
- テスト 87→90件（他のモデルへ流れない／429と503で案内が違う／失敗しても入力が残り再試行できる）。
  `npm run build` 通過。本番デプロイまで実施（実装の commit `%C%`）。

---

# 【経緯アーカイブ】

- **memo-okoshi-archive-2026-09.md** — 2026-09-07 P3: 確認・出力UIの本実装／2026-09-07 P3追補: こぼれの
  拾い上げ導線・UI正本の移管／2026-09-07 P2: 取り込み＋黒塗りUI／2026-09-07 P4: Vercelデプロイ／
  2026-09-07 P5: 組織語彙の辞書＋独自ドメイン／2026-09-08 P6: 実機フィードバックによる改良／
  **2026-09-09 P6-b: 文言とUIの細部**（P7で移動）／**2026-09-09 P6-c: 取り込み画面の作り直し**（台帳整理で移動）／
  **2026-09-09 P6-d: 使い方ページとアプリアイコン**（P7-bで移動）／**2026-09-09 P6-e: ステップ表示と取り込み配置**（P7-cで移動）／
  **2026-09-09 P6-f: 複数枚に気づける取り込み**（P7-dで移動）
- **memo-okoshi-archive-2026-09.md → 「決定の経緯（台帳から移動）」** — 2026-09-09の台帳整理で移した
  **フェーズ別の決定12節**（P6／P6-b〜P6-k／P7）＋**「原則・仕様」「組織語彙の辞書（P5）」**＋
  **「決定事項リスト（2026-08-17確定）」**。すべて原文のまま。
  ⚠️ **守るべきことの正本は台帳の「いま守る制約（主題別）」**。ここは「なぜそうしたか」を読むための資料で、
  移動時点の記述のまま更新しない。台帳と食い違ったら台帳が正しい。
  （2026-09-09に移動。原文のまま保存。
  移す前に点検し、位置連動が未実装でフックだけ残してあることを上へ昇格。こぼれの導線・UI正本の移管・
  pdf.jsのCDN固定・焼き込み送信・合言葉ゲートとVercelの運用手順・辞書の決定・独自ドメイン・将来枠は
  すでに上に載っていることを確認した。P6の決定は2026-09-09の台帳整理で「決定の経緯」へ移した）
- **memo-okoshi-archive-2026-08.md** — 2026-08-17 P0: 器の整備／2026-08-18 P1: 変換の核
  （2026-09-09に移動。原文のまま保存。移す前に「いまも効いている制約」を点検し、
  npm install のdeny運用・モデル一覧試行・変換30〜40秒はすでに上に載っていることを確認した）

（記録が10件を超えたら、最も古いものから原文のまま上記のファイルへ移す）
